---

dependencies:
  - role: nginx
    site_name: "{{ site_name }}"
    when: "'webservers' in group_names"

  - role: mysql
    db_user_name: root
    db_user_password: root123
    db_schema: "{{ site_name }}"
    when: "'dbservers' in group_names"
