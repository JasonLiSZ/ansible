---

# this is the test playbook, use > ansible-playbook ./playbooks/test.yml

- name: test playbook
  hosts: all
  tasks:
    - name: ping
      ping:
    - name: debug
      debug: msg={{ ansible_os_family }}

